# add-recipe-skill タスク

## 1. 投入順の要件を既存部分へ反映

- [x] 1.1 既存レシピ3件の材料が投入順になっているか点検し、なっていなければ並べ替える（要件: 材料の並びは投入順）
- [x] 1.2 ビルドが材料の順序を保持することを検証するテストを追加する（要件: 材料の並びは投入順）。プロパティベーステストで検証する性質: 任意の材料リストについて、検索用データに現れる材料の並びが入力ファイルの並びと同一（不変条件: 順序保持）
- [x] 1.3 レシピ表示が検索用データの順で材料を出していることを確認し、並べ替えがあれば取り除く（要件: 表示でも投入順が保たれる）

## 2. 聞き書きスキルの作成

- [x] 2.1 スキルの雛形を作り、聞き書きの流れ（語りからの抽出 → 不足の必須項目と調理方法だけ確認 → ID と全文をまとめて1回提示して承認）を記述する（要件: 語りから聞き取り、不足だけを確認する／生成前に全文の承認を得る。D-38）
- [x] 2.2 材料の扱いを記述する: 語られた順を投入順として保持、区分順への並べ替え禁止、順序が投入順と読めないときだけ確認、語られていない材料・分量・手順の補完禁止、「食材 分量」1行への整形（要件: 材料は語られた順を投入順として保持する／語られていない内容を補わない。D-39）
- [x] 2.3 ID 候補の流儀（基本ヘボン式・外来語は英語綴り）と、既存レシピとの衝突検知・別候補の提示を記述する（要件: レシピの ID は候補を提示し著者が承認する。D-40）
- [x] 2.4 生成後の検証を記述する: ビルドの実行（mise exec 経由の規約に従う）、失敗時はスキルが自ら修正して再ビルド、読みの警告の提示、口述された読みだけを対応表の訂正へ書き取り、コミットはせず報告で終える（要件: 生成後にビルドで検証する／読みの警告は提示し、口述された訂正だけを書き取る／コミットは行わない。D-41・D-42）

## 3. 動作確認

- [x] 3.1 実際のレシピ1件をスキルで聞き書きし、生成 → ビルド検証 → 報告（コミットしない）までの流れが仕様どおり動くことを確かめる（要件: recipe-capture 全体。とくに自動メニュー番号の先頭ゼロが保たれること）
- [x] 3.2 lint・format チェック・テストをすべて通す（apply 時の運用規約）
